#!/bin/bash

echo "diff ${2} and ${5}"
sleep 0.4

if [ "$(command -v nvim)" ]; then
	open="nvim -d"
else
	open="vimdiff"
fi

[ "$(file -bi "${2}" | sed -e "s/.*charset=\(.*\)$/\1/")" == binary ] && open=":"

${open} "${2}" "${5}"

