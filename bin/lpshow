#!/bin/bash

if [ -z $1 ]; then
	echo "Yo Yo"
	exit
fi

F=${1%.*}

sed -ie "s/､/, /g" ${F}.tex
sed -ie "s/｡/./g" ${F}.tex

platex ${F}.tex
dvipdfmx ${F}.dvi
rm `ls | grep -Ev ${F}\.\(tex$\)\|\(pdf$\) | grep ${F}`

evince ${F}.pdf
