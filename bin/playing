#!/bin/bash

NOWPLAY=`mocp -Q "%artist\ -\ %song\ （%album）" | sed -e "s/(\(.*\))/（\1）/g"`

PASSWORD=hogehoge
# or read from encrypted file

echo ${PASSWORD} | sudo -S -p "" sh -c "echo Now Playing : ${NOWPLAY} > /dev/kmsg"
